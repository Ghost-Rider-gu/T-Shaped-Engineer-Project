spring:
  application:
    name: api-gateway-service
  cloud:
    config:
      uri: http://config-server:9099
      fail-fast: true
      password: ${CONFIG_SERVER_PASSWORD}
      username: ${CONFIG_SERVER_USER_NAME}
