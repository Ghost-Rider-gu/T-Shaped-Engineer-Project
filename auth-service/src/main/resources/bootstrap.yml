spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:9099
      fail-fast: true
      password: ${CONFIG_SERVER_PASSWORD}
      username: ${CONFIG_SERVER_USER_NAME}
